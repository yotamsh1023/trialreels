VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_GETLATE_API_KEY=your-getlate-api-key
SUPABASE_ACCESS_TOKEN=your-supabase-personal-access-token

# GetLate: same key used for OAuth connect and for Analytics (post list). Analytics may require the Analytics add-on in GetLate.

# Optional: RapidAPI Instagram scraper – used to load the user's existing Instagram reels (previous posts) in addition to GetLate.
# Without this, only posts published via GetLate (and Analytics external sync) are shown.
# VITE_RAPIDAPI_KEY=your-rapidapi-key
# VITE_RAPIDAPI_HOST=instagram-api-fast-reliable-data-scraper.p.rapidapi.com